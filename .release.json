{
  "src": {
    "beforeStageCommand": "rm -rf dist && mkdir dist && touch ./dist/foo.md"
  },
  "commitMessage": "release %s",
  "tagAnnotation": "release %s",
  "github": {
    "release": false,
    "releaseName": "release %s"
  }
}
